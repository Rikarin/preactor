{
  "name": "@rikarin/preactor",
  "description": "Interop between preact and C#",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rikarin/preactor.git"
  },
  "version": "0.0.19",
  "main": "./dist/index.js",
  "types": "./typings.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p ./tsconfig.json",
    "start": "tsc -w -p ./tsconfig.json",
    "format": "prettier . --write",
    "pub": "npm run clean && npm run build && npm publish --access public"
  },
  "dependencies": {
    "css-flatten": "^2.0.0",
    "css-simple-parser": "^3.0.0",
    "typescript": "^5.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.1",
    "fs-extra": "^11.2.0",
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0",
    "prettier": "^3.3.3",
    "rimraf": "^5.0.7",
    "tailwindcss": "^3.4.11",
    "tiny-glob": "^0.2.9"
  }
}
